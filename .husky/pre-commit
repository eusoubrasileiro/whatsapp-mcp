# Fast gate (~15-30s). Runs on every commit — catches type and unit-level
# regressions without blocking the inner loop. Stages, in order:
#   1. lint-staged   — biome auto-fix on staged files
#   2. tsc --noEmit  — full project type-check (catches cross-file effects)
#   3. test:coverage — unit tests; produces coverage-summary.json for stage 4
#   4. quality-gate  — deterministic metrics ratchet vs quality-baseline.json
#
# The LLM reviewer is deliberately NOT here: it lives on pre-push only, so a
# Claude CLI outage can never block a local commit (ratified 2026-07-22).
pnpm exec lint-staged
pnpm exec tsc --noEmit
pnpm test:coverage --silent
pnpm quality-gate
